# Mobile Gateway (FastAPI)

Mobile-specific API gateway with mobile-focused contracts.

## Exposed endpoints

- `GET /mobile/health`
- `POST /mobile/scan/upn`

Unlike the web gateway, this gateway exposes only mobile-focused endpoints and returns a compact response optimized for mobile clients.
